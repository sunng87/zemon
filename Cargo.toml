[package]
name = "zemon"
version = "0.1.0"
edition = "2021"

[dependencies]
ratatui = "0.26"
crossterm = "0.27"
clap = { version = "4.0", features = ["derive"] }
sysinfo = "0.35"
chrono = "0.4"

[profile.release]
strip = true
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
